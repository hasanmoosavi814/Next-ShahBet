import { Card, CardHeader, CardTitle } from "@/components/ui/card";

const ArticleEmpty = () => (
  <Card>
    <CardHeader>
      <CardTitle>No related articles found.</CardTitle>
    </CardHeader>
  </Card>
);

export default ArticleEmpty;
